#0 Upewnij się że masz ustawione adresy DNS

sudo nano /etc/systemd/resolved.conf

[Resolve]
DNS=1.1.1.1 8.8.8.8
FallBackDNS=1.0.0.1 8.4.4.8


------ Ustawienie strefy czasowej (Europa/Warszawa) -------------------
timedatectl
sudo timedatectl set-timezone Europe/Warsaw
sudo timedatectl status


1) Aktualizacja systemu
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl gnupg lsb-release apt-transport-https ca-certificates pwgen


2) Zainstaluj Java (OpenJDK 17)

Graylog i OpenSearch działają dobrze na OpenJDK 17.

sudo apt install -y openjdk-17-jre-headless
java -version


